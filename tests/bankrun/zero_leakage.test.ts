// zero leakage — stub, real assertions land in the
// sprint that implements the corresponding instruction(s). See IVM in the
// project docs for the exact invariant IDs this file is responsible for.
import { describe, it } from "mocha";

describe("zero_leakage", () => {
  it.skip("TODO: implement once the corresponding instruction handler lands", () => {});
});
